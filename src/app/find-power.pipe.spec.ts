import { FindPowerPipe } from './find-power.pipe';

describe('FindPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new FindPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
